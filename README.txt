We find original credentials in this order

Loaded from environment variables,
Loaded from the shared credentials file (~/.aws/credentials),
Loaded from IAM roles for Amazon EC2 (if running on EC2)

Region defaults to us-east-1 or use the AWS_REGION variable to override

Output is env variable set of new credentials
